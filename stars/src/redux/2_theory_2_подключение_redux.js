/**
 * Подключение библиотеки Redux
 * 
 * npm install redux react-redux.
 * 
 * После установки библиотеки нужно её настроить
 */

/**
 * Библиотека react-redux предоставляет разработчикам специальный компонент Provider (почти такой же, как в React Context),
 * HOC connect и набор хуков, которые позволяют удобно работать с Redux в функциональных компонентах.
 * С помощью этого инструментария можно подключить хранилище к отдельным компонентам и использовать его данные: превращать их в пропсы и внутреннее состояние.
 */

/**
 * import {createStore} from "redux" is depreced
 * Вместо неё нужно использовать 
 * import {cofigureStore} from "reduxjs/toolkit";
 * 
 * из npm i @reduxjs/toolkit
//  * npm install @reduxjs/toolkit 
 */